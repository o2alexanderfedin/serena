---
name: $skill_name
description: $description
type: skill
---

# $title

$summary

## When to use

Invoke `${facade}` (language: **$language**) when the user says any of:

$trigger_list

> v2.0 wire-name cleanup: the legacy alias `scalpel_${facade}` continues to
> work through v2.x and is removed in v2.1. Prefer the unprefixed name in
> new prompts.

## How it works

The facade composes the following LSP primitives in order:

$primitive_list

## Tool call

```json
{"tool": "${facade}", "arguments": {"path": "<file>", "language": "$language"}}
```
