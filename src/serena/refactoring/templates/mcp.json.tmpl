{
  "mcpServers": {
    "lsp": {
      "command": "uvx",
      "args": [
        "--from", "$source",
        "serena", "start-mcp-server",
        "--project-from-cwd",
        "--server-name", "scalpel-$language"
      ],
      "env": {}
    }
  }
}
